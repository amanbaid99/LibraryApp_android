<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Online Library"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User.header_layout"></activity>
        <activity android:name=".User.Profile" />
        <activity android:name=".Admin.Addbooks" />
        <activity android:name=".Admin.AdminHome" />
        <activity android:name=".Admin.AdminBookdetails" />
        <activity android:name=".Admin.AdminSearch" />
        <activity android:name=".User.Bookdetailslayouthome" />
        <activity android:name=".User.SearchPage" />
        <activity android:name=".signin.login.Login" />
        <activity android:name=".User.MainActivity" />
        <activity android:name=".signin.login.Signup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>